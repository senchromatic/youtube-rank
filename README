Install:
$ sudo apt-get install libssl-dev
$ Rscript scripts/install.R

Run examples:
$ Rscript scripts/examples.R
(if working in RStudio, setwd to this repository's root directory)

Open output files:
$ bash show.sh
